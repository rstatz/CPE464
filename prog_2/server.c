#include <stdint.h>

#include "chat_parse.h"
#include "client_table.h"

int main(int argc, char* argv[]) {
    // setup socket/handle table
    Client_Table* table = new_ctable();

    // open server with port number
    // add server socket to table

    return 0;
}
