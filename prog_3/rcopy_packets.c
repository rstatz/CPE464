#include <stdint.h>
#include <sys/socket.h>

#include "networks.h"
#include "rcopy_packets.h"

static void send_header_pack(int sock, int seq, int flag) {
    
}

void send_setup(int sock) {
    //send_header_pack(int sock);
}

